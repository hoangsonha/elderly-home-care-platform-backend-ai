# Phase 1: Rule-based matching
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0
pydantic-settings==2.1.0
python-multipart==0.0.6
numpy>=1.26.0

# Phase 2: Semantic matching với PhoBERT (KHÔNG DÙNG - đã bỏ skills matching)
# transformers>=4.30.0
# Use CPU-only torch to reduce image size (from ~10GB to ~2GB)
# Note: Dockerfile will install torch from CPU-only index
# torch>=2.0.0
# scikit-learn>=1.3.0

# Alternative: Sentence Transformers (uncomment nếu muốn dùng thay PhoBERT)
# sentence-transformers==2.2.2

# Database (optional cho Phase 1)
# sqlalchemy==2.0.23
# psycopg2-binary==2.9.9
